package AbstractClass;

//partial abstract, abstract class
public abstract class MobileUser {
 void call() { //this is non abstract method /this method has implementation //default method,No modifier
	 System.out.println("this is call method");
 }
 abstract void sendMassage() ;//this is abstract method and it is method declaration because there has no body
	 
 
}
